/**
 * Firebase package.
 *
 * <p>Contains the generic Firestore helper service that wraps
 * the Firebase Admin SDK, providing reusable CRUD operations
 * consumed by all repository classes.
 * <ul>
 *   <li>FirestoreService — Generic Firestore CRUD abstraction</li>
 * </ul>
 */
package com.internvision.portal.firebase;
